# GCP Network Module (Skeleton)

Creates VPC, subnets, and firewall baseline for OAL platform workloads.

Expected outputs:
- `vpc_id`
- `subnet_ids`
- `pods_secondary_range`
- `services_secondary_range`
