# GCP IAM Module (Skeleton)

Defines runtime identities for workloads and access bindings for:
- Secret Manager read access
- Artifact Registry pull access
- Optional observability export permissions
