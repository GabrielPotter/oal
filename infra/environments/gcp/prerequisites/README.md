# GCP Prerequisites

Use the target-aware lifecycle checker for dependency validation and optional install.

`infra/lifecycle/prerequisites/check-install.sh --target gcp`

This includes checks for:
- gcloud CLI
- kubectl
- kustomize (or kubectl kustomize)
- terraform
