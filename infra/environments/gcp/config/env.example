# Environment scope: gcp
# Consumer: lifecycle deploy scripts and terraform/k8s tooling.
# Secret handling: keep credentials in gcloud auth/workload identity, not in this file.

REGISTRY_PREFIX=us-central1-docker.pkg.dev/my-project/oal
IMAGE_TAG=
TF_VAR_project_id=my-project
TF_VAR_region=us-central1
TF_VAR_cluster_name=oal-cluster
